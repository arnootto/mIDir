% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ML.MmID.R
\name{ml.MmIDir}
\alias{ml.MmIDir}
\title{Finite Mixture of Mode-Parameterized Inverted Dirichlet Distributions}
\usage{
ml.MmIDir(
  X,
  k,
  initialization = "kmeans",
  seed = NULL,
  start.z = NULL,
  method = "BFGS",
  maxit = 1000,
  reltol = 1e-15,
  threshold = 10^-4,
  max.iter = 5000,
  plotll = TRUE
)
}
\arguments{
\item{X}{Numeric matrix with \code{d} columns and \code{n} rows, where each row represents a vector of positive values.}

\item{k}{Integer, the number of mixture components (must be positive).}

\item{initialization}{Character string specifying the initialization method for posterior probabilities. Options are \code{"mclust"}, \code{"kmeans"}, \code{"random.soft"} (random soft assignments), \code{"random.hard"} (random hard assignments), or \code{"manual"} (uses \code{start.z}). Default is \code{"kmeans"}.}

\item{seed}{Integer, seed for random number generation if \code{initialization} is \code{"random.soft"} or \code{"random.hard"}. Default is \code{NULL}.}

\item{start.z}{Numeric matrix of size \code{n x k}, initial posterior probabilities for \code{initialization = "manual"}. Can be soft or hard assignments. Default is \code{NULL}.}

\item{method}{Optimization method within the M-step. Default is \code{"BFGS"}. Other options include \code{"Nelder-Mead"}.}

\item{maxit}{Maximum number of iterations for \code{optim} within step. Default is 1000.}

\item{reltol}{Relative convergence tolerance for \code{optim} within step. Default is 1e-15.}

\item{threshold}{Numeric, convergence threshold for the log-likelihood difference. Default is 1e-4.}

\item{max.iter}{Maximum number of EM iterations. Default is 5000.}

\item{plotll}{Logical; if \code{TRUE}, plots the log-likelihood over iterations. Default is \code{TRUE}.}
}
\value{
An object of class \code{"mixIDir"} (list) containing:
\describe{
  \item{X}{The input matrix \code{X}.}
  \item{k}{Number of mixture components.}
  \item{n}{Number of observations.}
  \item{density}{Matrix of size \code{n x k}, the weighted density for each observation and component.}
  \item{z}{Matrix of size \code{n x k}, posterior probabilities for each observation and component.}
  \item{prior}{Vector of length \code{k}, estimated mixing proportions.}
  \item{theta}{Matrix of size \code{d x k}, estimated mode parameters for each component.}
  \item{gamma}{Vector of length \code{k}, estimated dispersion parameters for each component.}
  \item{group}{Vector of length \code{n}, cluster assignments based on maximum posterior probability.}
  \item{iter.stop}{Number of EM iterations performed.}
  \item{npar}{Number of parameters estimated: \code{(k-1) + k*d}.}
  \item{loglik}{Final log-likelihood.}
  \item{AIC}{Akaike Information Criterion.}
  \item{BIC}{Bayesian Information Criterion.}
  \item{call}{The matched call.}
}
}
\description{
Performs maximum likelihood estimation for a fintie mixture of mode-parameterized inverted Dirichlet distributions using the Expectation-Maximization (EM) algorithm.
}
\details{
The \code{ML.MmIDir} function fits a mixture of \code{k} mode-parameterized inverted Dirichlet distributions to the data \code{X} using the EM algorithm.
}
