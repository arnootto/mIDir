% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcMID.R
\name{dcMID}
\alias{dcMID}
\title{Contaminated Inverted Dirichlet Density}
\usage{
dcMID(x, theta, gamma, delta, eta)
}
\arguments{
\item{x}{Numeric matrix or data frame with \code{d} columns, where each row represents a vector of positive values. The number of columns must be one more than the length of \code{theta}.}

\item{theta}{Numeric vector of length \code{d-1}, the mode parameters (non-negative).}

\item{gamma}{Numeric scalar, the variance parameter for the good component (positive).}

\item{delta}{Numeric scalar, the mixing proportion (between 0 and 1).}

\item{eta}{Numeric scalar, the inflation parameter for the contaminated component (positive).}

\item{log}{Logical; if \code{TRUE}, returns the log-density. If \code{FALSE}, returns the density. Default is \code{FALSE}.}
}
\value{
A numeric vector of density values (if \code{log = FALSE}) or log-density values (if \code{log = TRUE}), one for each row of \code{x}.
}
\description{
Computes the density (or log-density) of the contaminated mode-parameterized inverted Dirichlet distribution.
}
\details{
The contaminated inverted Dirichlet distribution has density:
\deqn{
f(x; \theta, \gamma, \delta, \eta) = (1-\delta) \cdot f_{\text{MID}}(x; \theta, \gamma) + \delta \cdot f_{\text{MID}}(x; \theta, \eta \gamma)
}
where \eqn{f_{\text{MID}}(x; \theta, \gamma)} is the density from \code{dMID}.
}
\examples{
x <- matrix(c(1, 2, 3, 4, 5, 6), nrow = 2, byrow = TRUE)
theta <- c(0.2, 0.3)
gamma <- 0.5
delta <- 0.2
eta <- 2
dcMID(x, theta, gamma, delta, eta, log = FALSE)
dcMID(x, theta, gamma, delta, eta, log = TRUE)

}
